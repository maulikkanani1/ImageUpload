var AWS = require('aws-sdk');

module.exports = {
	aws : {
		accessKeyId: 'accessKeyId',
		secretAccessKey: 'secretAccessKey',
		region: 'region',
		bucket: 'bucketName'
	},
	mongoDB : {
		URL : "mongo_url",
		dbName : "dbName"
	}
}
